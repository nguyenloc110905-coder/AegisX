# AegisX Agent

The agent will identify a Linux device, collect host telemetry through isolated collectors, normalize events, buffer them locally, and deliver them to the API.

It will not detect every event with AI, remediate the host, or run arbitrary commands. Executable agent code begins in Milestone 2; this directory is documentation-only in Milestone 0.
