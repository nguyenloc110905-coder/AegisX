# Shared Contracts

This directory is reserved for versioned schemas and generated or language-neutral contracts shared by more than one AegisX application.

Application business logic, database access, OS-specific collection code, and UI components do not belong here. Shared artifacts are introduced only when a real cross-application consumer exists.
